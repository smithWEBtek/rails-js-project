<%= form_for @project do |f| %>
<%= f.label :name "Select Name" %><br>
<%= f.text_field :name %><br>

<%= f.label :complete, "Is the project complete?" %>
<%= f.password_field :complete %>
<%= f.submit, "Create Project" %>
<%= end %>
