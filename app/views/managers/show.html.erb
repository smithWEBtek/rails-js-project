<h1>Manager Profile Below:</h1>
<h2>Name:<%= @manager.name %></h2>
<h2>Email:<%= @manager.email %></h2><br>

<h3>Shortcuts:</h3>
<%= button_to 'Edit Profile', edit_manager_path(@manager), :method => :get %><br>
<%= button_to 'Create Client', new_client_path, :method => :get %><br>
<%= button_to 'Create Project', new_project_path, :method => :get %><br>
<button id="DisplayClients">Display Clients</button> <!-- req 1 -->
  <div >
    <ul id="clients">
    </ul>
  </div>

<!-- <h4>Clients:</h4>
    <ul>
     @manager.clients.each do |client|
    <li><link_to client.name, client_path(client) </li>
       end
    </ul> -->
    <script>
    $(document).ready(function(){
      $("#DisplayClients").on("click", function(){
        var current_path = window.location.pathname;

        $.get(current_path +`/clients`).done (function(clients) {
        debugger
        alert("The paragraph was clicked.");
        });
      });
    });
    </script>
